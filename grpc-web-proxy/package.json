{
  "name": "grpc-web-proxy",
  "version": "1.0.0",
  "description": "HTTP to gRPC proxy for BI-Index",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "@grpc/grpc-js": "^1.8.0",
    "@grpc/proto-loader": "^0.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
