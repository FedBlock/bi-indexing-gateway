# idxmngr 기본 설정 파일
# 이 파일은 idxmngr 서버가 시작할 때 필요합니다.

# 서버 설정
server:
  port: 50052
  host: "localhost"

# 데이터베이스 설정
database:
  type: "file"
  path: "./data"

# 로깅 설정
logging:
  level: "info"
  file: "./idxmngr.log"

# 네트워크 설정 파일 경로
network_config:
  path: "./config/network_config.yaml"
